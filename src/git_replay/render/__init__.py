"""SVG/HTML renderers for git-replay widgets.

Each module in this package turns aggregated commit data (see
:mod:`git_replay.aggregate`) into self-contained, static markup. Renderers own
all presentation concerns — layout, palette, and scaling — while the data layer
stays pure.
"""
